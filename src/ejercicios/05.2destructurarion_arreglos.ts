const arrDbz: string[] = ["Goku", "Vegeta", "Trunks"];

//destructurar un arreglo, toma los valores por la posicion en el arreglo
const [p1, p2, p3] = arrDbz;

//si solo queremos un valor espeficido, colcoamos coma para las posiciones ue no queremos
const [, , x] = arrDbz;

